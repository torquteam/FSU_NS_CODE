class quark {
public:
    double pr(double cp, double mass, double deg);
    double en(double cp, double mass, double deg);
    double nb(double cp, double mass, double deg);
    double NRpr(double cp, double mass, double deg);
    double NRen(double cp, double mass, double deg);
    double NRnb(double cp, double mass, double deg);
};